{% comment %}
  Placeholder section: spa-header
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-header' }}</p>
</div>

{% schema %}
{
  "name": "spa-header",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-header"
    }
  ],
  "presets": [{ "name": "spa-header" }]
}
{% endschema %}
