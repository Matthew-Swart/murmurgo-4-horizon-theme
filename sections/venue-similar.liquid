{% comment %}
  Placeholder section: venue-similar
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'venue-similar' }}</p>
</div>

{% schema %}
{
  "name": "venue-similar",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "venue-similar"
    }
  ],
  "presets": [{ "name": "venue-similar" }]
}
{% endschema %}
