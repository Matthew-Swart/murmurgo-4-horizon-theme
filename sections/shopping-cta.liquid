{% comment %}
  Placeholder section: shopping-cta
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'shopping-cta' }}</p>
</div>

{% schema %}
{
  "name": "shopping-cta",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "shopping-cta"
    }
  ],
  "presets": [{ "name": "shopping-cta" }]
}
{% endschema %}
