{% comment %}
  Placeholder section: venue-capacity
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'venue-capacity' }}</p>
</div>

{% schema %}
{
  "name": "venue-capacity",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "venue-capacity"
    }
  ],
  "presets": [{ "name": "venue-capacity" }]
}
{% endschema %}
