{% comment %}
  Placeholder section: shopping-map
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'shopping-map' }}</p>
</div>

{% schema %}
{
  "name": "shopping-map",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "shopping-map"
    }
  ],
  "presets": [{ "name": "shopping-map" }]
}
{% endschema %}
