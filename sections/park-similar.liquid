{% comment %}
  Placeholder section: park-similar
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'park-similar' }}</p>
</div>

{% schema %}
{
  "name": "park-similar",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "park-similar"
    }
  ],
  "presets": [{ "name": "park-similar" }]
}
{% endschema %}
