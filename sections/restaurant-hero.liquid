{% comment %}
  Placeholder section: restaurant-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'restaurant-hero' }}</p>
</div>

{% schema %}
{
  "name": "restaurant-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "restaurant-hero"
    }
  ],
  "presets": [{ "name": "restaurant-hero" }]
}
{% endschema %}
