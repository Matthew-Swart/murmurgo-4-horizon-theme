{% comment %}
  Placeholder section: stay-hero-gallery
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'stay-hero-gallery' }}</p>
</div>

{% schema %}
{
  "name": "stay-hero-gallery",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "stay-hero-gallery"
    }
  ],
  "presets": [{ "name": "stay-hero-gallery" }]
}
{% endschema %}
