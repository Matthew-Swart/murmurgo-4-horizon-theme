{% comment %}
  Placeholder section: bar-similar
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'bar-similar' }}</p>
</div>

{% schema %}
{
  "name": "bar-similar",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "bar-similar"
    }
  ],
  "presets": [{ "name": "bar-similar" }]
}
{% endschema %}
