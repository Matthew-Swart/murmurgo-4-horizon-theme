{% comment %}
  Placeholder section: park-header
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'park-header' }}</p>
</div>

{% schema %}
{
  "name": "park-header",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "park-header"
    }
  ],
  "presets": [{ "name": "park-header" }]
}
{% endschema %}
