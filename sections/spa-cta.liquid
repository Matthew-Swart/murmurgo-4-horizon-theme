{% comment %}
  Placeholder section: spa-cta
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-cta' }}</p>
</div>

{% schema %}
{
  "name": "spa-cta",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-cta"
    }
  ],
  "presets": [{ "name": "spa-cta" }]
}
{% endschema %}
