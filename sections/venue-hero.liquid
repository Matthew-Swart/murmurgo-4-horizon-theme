{% comment %}
  Placeholder section: venue-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'venue-hero' }}</p>
</div>

{% schema %}
{
  "name": "venue-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "venue-hero"
    }
  ],
  "presets": [{ "name": "venue-hero" }]
}
{% endschema %}
