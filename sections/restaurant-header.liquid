{% comment %}
  Placeholder section: restaurant-header
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'restaurant-header' }}</p>
</div>

{% schema %}
{
  "name": "restaurant-header",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "restaurant-header"
    }
  ],
  "presets": [{ "name": "restaurant-header" }]
}
{% endschema %}
