{% comment %}
  Placeholder section: bar-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'bar-hero' }}</p>
</div>

{% schema %}
{
  "name": "bar-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "bar-hero"
    }
  ],
  "presets": [{ "name": "bar-hero" }]
}
{% endschema %}
