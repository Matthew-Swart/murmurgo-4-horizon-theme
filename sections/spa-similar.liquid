{% comment %}
  Placeholder section: spa-similar
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-similar' }}</p>
</div>

{% schema %}
{
  "name": "spa-similar",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-similar"
    }
  ],
  "presets": [{ "name": "spa-similar" }]
}
{% endschema %}
