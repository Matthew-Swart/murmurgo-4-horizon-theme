{% comment %}
  Placeholder section: spa-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-hero' }}</p>
</div>

{% schema %}
{
  "name": "spa-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-hero"
    }
  ],
  "presets": [{ "name": "spa-hero" }]
}
{% endschema %}
