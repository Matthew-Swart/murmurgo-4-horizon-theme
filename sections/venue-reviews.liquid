{% comment %}
  Placeholder section: venue-reviews
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'venue-reviews' }}</p>
</div>

{% schema %}
{
  "name": "venue-reviews",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "venue-reviews"
    }
  ],
  "presets": [{ "name": "venue-reviews" }]
}
{% endschema %}
