{% comment %}
  Placeholder section: shopping-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'shopping-hero' }}</p>
</div>

{% schema %}
{
  "name": "shopping-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "shopping-hero"
    }
  ],
  "presets": [{ "name": "shopping-hero" }]
}
{% endschema %}
