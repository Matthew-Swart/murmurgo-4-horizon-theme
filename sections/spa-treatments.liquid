{% comment %}
  Placeholder section: spa-treatments
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-treatments' }}</p>
</div>

{% schema %}
{
  "name": "spa-treatments",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-treatments"
    }
  ],
  "presets": [{ "name": "spa-treatments" }]
}
{% endschema %}
