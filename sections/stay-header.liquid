{% comment %}
  Placeholder section: stay-header
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'stay-header' }}</p>
</div>

{% schema %}
{
  "name": "stay-header",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "stay-header"
    }
  ],
  "presets": [{ "name": "stay-header" }]
}
{% endschema %}
