{% comment %}
  Placeholder section: bar-details
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'bar-details' }}</p>
</div>

{% schema %}
{
  "name": "bar-details",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "bar-details"
    }
  ],
  "presets": [{ "name": "bar-details" }]
}
{% endschema %}
