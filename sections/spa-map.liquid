{% comment %}
  Placeholder section: spa-map
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'spa-map' }}</p>
</div>

{% schema %}
{
  "name": "spa-map",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "spa-map"
    }
  ],
  "presets": [{ "name": "spa-map" }]
}
{% endschema %}
