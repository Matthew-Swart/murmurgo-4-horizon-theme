{% comment %}
  Placeholder section: park-hero
  TODO: Implement actual section content.
{% endcomment %}

<div class="section-placeholder" data-section-id="{{ section.id }}" style="padding: 2rem; text-align: center; color: #999;">
  <p>{{ section.settings.placeholder_text | default: 'park-hero' }}</p>
</div>

{% schema %}
{
  "name": "park-hero",
  "settings": [
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Placeholder text",
      "default": "park-hero"
    }
  ],
  "presets": [{ "name": "park-hero" }]
}
{% endschema %}
